# npm audit — 2026-05-17

**Branch:** `hygiene/audit-cleanup-2026-05-17`
**Author:** ak (Session 2 — audit hygiene)
**Command:** `npm audit --omit=dev`

---

## Before

5 vulnerabilities (2 moderate, 2 high, 1 critical) — all production deps.

| # | Package | Severity | Issue | Fix path |
| -- | -- | -- | -- | -- |
| 1 | `fast-xml-parser` (via `@aws-sdk/xml-builder` → `@aws-sdk/client-s3`) | **critical** | Six advisories: entity-encoding bypass, DoS via entity expansion, stack overflow in XMLBuilder, numeric entity expansion bypass (CVE-2026-26278), entity-expansion limit bypass when set to zero, XML comment + CDATA injection in XMLBuilder. | `npm audit fix` (5.3.4 → 5.7.3) |
| 2 | `minimatch` | **high** | Three ReDoS advisories (repeated wildcards, GLOBSTAR backtracking, nested extglobs). | `npm audit fix` (9.0.5 → 9.0.9) |
| 3 | `path-to-regexp` | **high** | ReDoS via multiple route parameters (Express 4 transitive dep). | `npm audit fix` (0.1.12 → 0.1.13) |
| 4 | `brace-expansion` | moderate | Zero-step sequence causes process hang + memory exhaustion. | `npm audit fix` (2.0.2 → 2.1.0) |
| 5 | `@aws-sdk/xml-builder` | moderate | Pinned to vulnerable fast-xml-parser range — auto-resolved by #1. | Transitive via #1. |

## Action

```bash
npm audit fix
```

Result: `added 4 packages, changed 11 packages, and audited 676 packages — found 0 vulnerabilities`.

No `npm audit fix --force` was used. No breaking-change upgrades required.

## After

```
$ npm audit --omit=dev
found 0 vulnerabilities
```

## Lockfile changes

`package-lock.json` only (no `package.json` changes since all fixes were
non-breaking patch / minor bumps already permitted by existing semver ranges).

### Version bumps

- `brace-expansion`        2.0.2  → 2.1.0
- `fast-xml-parser`        5.3.4  → 5.7.3
- `minimatch`              9.0.5  → 9.0.9
- `path-to-regexp`         0.1.12 → 0.1.13
- `@aws-sdk/xml-builder`   3.972.18 → 3.972.24
- (plus minor deduped bumps to `@smithy/types`, `picomatch`, `pdfkit`, `strnum`,
  `xtend`, `path-is-absolute`, `@paralleldrive/cuid2`)

### New transitive packages (all under `@aws-sdk/xml-builder`)

`npm ls` confirms these are legitimate transitive deps of the upgraded
`fast-xml-parser@5.7.3`:

```
booking-backend@1.0.0
`-- @aws-sdk/client-s3@3.990.0
  `-- @aws-sdk/core@3.973.10
    `-- @aws-sdk/xml-builder@3.972.24
      +-- @nodable/entities@2.1.0
      `-- fast-xml-parser@5.7.3
        +-- @nodable/entities@2.1.0 deduped
        +-- fast-xml-builder@1.2.0
        | +-- path-expression-matcher@1.5.0 deduped
        | `-- xml-naming@0.1.0
        `-- path-expression-matcher@1.5.0
```

Not typosquats — all under the upgraded `@aws-sdk/xml-builder` subtree.

## Follow-up

None required. All advisories closed by patch/minor upgrades.

If future audits flag breaking-change upgrades, those should be batched into
a dedicated dependency-upgrade PR (do NOT run `npm audit fix --force` in a
hygiene PR like this one).
