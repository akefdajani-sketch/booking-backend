# Contributing (Backend)

## Guardrails
- Service defines time.
- Backend is truth for availability.
- Multi-slot bookings must remain contiguous.
- Duration = selected slots × slot interval.
- Always enforce tenant scoping.

## Tests
Run: `npm test`
